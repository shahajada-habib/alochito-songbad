# MYSQL DEV DATABASE (PERSISTENT LOCAL DEVELOPMENT)
# Run with: SPRING_PROFILES_ACTIVE=mysql-dev
# Unlike the default dev profile, this uses real local MySQL and keeps data after backend restart.

spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/alochito_songbad?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:root}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Use Flyway migrations as the source of truth for MySQL schema.
spring.flyway.enabled=true
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Dev seed users are enabled here so admin/editor/reporter login still works locally.
app.seed.default-users.enabled=true
app.seed.default-password=${SEED_DEFAULT_PASSWORD:1234}
app.seed.default-users.reset-password=true

app.jwt.secret=${JWT_SECRET:alochito-songbad-mysql-dev-secret-change-before-production}
app.jwt.expiration-seconds=${JWT_EXPIRATION_SECONDS:86400}
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:4200,http://127.0.0.1:4210}
app.media.upload-dir=${MEDIA_UPLOAD_DIR:uploads}
